# Renderers

One page per renderer, created from `_template.md` when the renderer package is added. See `../architecture.md` for the renderer contract and the registry's fallback policy, and `../roadmap.md` for the planned set and order.

| Renderer | Package | Page |
|---|---|---|
| code / text (Monaco) | `@prism/renderer-code` | [code.md](code.md) |
| images / SVG | `@prism/renderer-image` | [image.md](image.md) |
